/**
 * ADDRESS ROUTES
 *
 * This is where we define all the routes for the Address feature.
 * These routes get exported to the global /routes.js file.
 */

'use strict';

// Returns a function that attaches Address feature routes to the global router object
module.exports = (passport, router) => {

  // return router
  return router;
};
